package model;

/**
 * Constants for maze construction.
 */
public class MazeConstants {
    /** The default number of rows in the maze. */
    public static final int DEFAULT_NUM_ROWS = 20;
    
    /** The default number of columns in the maze. */
    public static final int DEFAULT_NUM_COLS = 20;
    
    /** The minimum number of rows allowed in the maze. */
    public static final int MIN_NUM_ROWS = 5;
    
    /** The minimum number of columns allowed in the maze. */
    public static final int MIN_NUM_COLS = 5;
    
    /** The maximum number of rows allowed in the maze. */
    public static final int MAX_NUM_ROWS = 30;
    
    /** The maximum number of columns allowed in the maze. */
    public static final int MAX_NUM_COLS = 50;
}
